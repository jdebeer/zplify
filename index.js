
const zplify = (function() {
    
  return {
    generateTable: function({ columnDefinitions, tableRowData, config }) {

      // config can include margin sizes, text size, and padding in cells

      return 'working';
    }
  }
})();

module.exports = zplify;
